import { AppSoon } from "@/components/AppSoon";
export default function TradePage() {
  return <AppSoon title="Trade" blurb="Swap DRIP / USDG and schedule buys. Wires to the DRIP/USDG pool at launch." />;
}
