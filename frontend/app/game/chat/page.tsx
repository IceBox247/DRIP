import { AppSoon } from "@/components/AppSoon";
export default function ChatPage() {
  return <AppSoon title="Chat" blurb="Live miner chat. Connect a wallet to post once the backend is live (Phase 2)." />;
}
