import { AppSoon } from "@/components/AppSoon";
export default function MorePage() {
  return <AppSoon title="More" blurb="Explore stats, reserve, rewards and docs — coming as the app fills out." />;
}
